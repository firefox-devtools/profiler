machine:
  node:
    version: 7.8
dependencies:
  override:
    - yarn install
test:
  pre:
    - yarn license-check
    - yarn eslint
    - yarn flow -- --quiet
  override:
    - yarn test:quiet -- --coverage --runInBand
  post:
    - bash <(curl -s https://codecov.io/bash)
